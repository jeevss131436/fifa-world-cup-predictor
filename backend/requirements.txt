# Unpinned so prebuilt wheels are picked for your Python version.
# (Pinned older versions force a source build of pydantic-core on Python 3.14.)
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
torch>=2.0
